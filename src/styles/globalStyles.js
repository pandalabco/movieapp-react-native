import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  fullScreen: { flex: 1 },
  moviesSlider: {
    height: 260
  },
  moviesSliderTitle: {
    fontSize: 30,
    fontWeight: "bold"
  }
})
